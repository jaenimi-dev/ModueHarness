[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "modue-harness"
version = "0.8.0"
description = "Multi-AI CLI Collaboration Harness - A flexible harness framework"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
authors = [
    { name = "JeaMin Lim" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
]
dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
ui = [
    "nicegui>=1.4.0",
    "textual>=0.50.0",
]
all = [
    "pytest>=7.0",
    "nicegui>=1.4.0",
    "textual>=0.50.0",
]

[project.scripts]
modue-harness = "modue_harness.cli:main"

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra -q"
testpaths = ["tests"]
pythonpath = ["src"]
